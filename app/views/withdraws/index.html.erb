<% @withdraws.each do |withdraw| %>
  <div class="col-md-6 col-md-offset-3 text-center">
    <div class="panel panel-default">
      <div class="panel-body">
        <div class="col-md-4">
          <% if withdraw.status.present? %>
            <span class="glyphicon glyphicon-ok"></span><br>
            <label style="background-color: #8DC63F; color: #ffffff">Telah Dicairkan</label>
          <% else %>
            <span class="glyphicon glyphicon-refresh"></span><br>
            <label style="background-color: #D7DF23; color: #ffffff">Sedang Dalam Proses</label>
          <% end %>
        </div>
        <div class="col-md-8">
          <strong>Withdraw Amount:</strong> <%= withdraw.amount %><br>
          <strong>Withdraw Requested at:</strong> <%= withdraw.created_at.strftime("%d %B %Y at %H:%M") %><br>
        </div>
      </div>
    </div>
  </div>
<% end %>
<div class="text-center">
  <%= link_to 'Back', bangsaku_path, class: 'btn btn-green' %>
</div>
