<% @orders.each do |order| %>
  <div class="col-md-6 col-md-offset-3 text-center">
    <div class="panel panel-default">
      <div class="panel-body">
        <div class="col-xs-4">
          <% if order.status.present? %>
            <span class="glyphicon glyphicon-ok"></span><br>
            <label style="background-color: #8DC63F; color: #ffffff">Telah Dijemput</label>
          <% else %>
            <span class="glyphicon glyphicon-refresh"></span><br>
            <label style="background-color: #D7DF23; color: #ffffff">Sedang Dalam Proses</label>
          <% end %>
        </div>

        <div class="col-xs-8">
          <%= order.created_at.strftime("%d %B %Y at %H:%M") %><br>

          <% if order.price.present? %>
            <strong>Price:</strong> Rp<%= number_with_delimiter(order.price, delimiter: '.') %><br>
          <% end %>
          <% if order.weight.present? %>
            <strong>Weight:</strong> <%= order.weight %> kg<br>
          <% end %>

          <br>

          <%= link_to 'View Detail', order, class: 'btn btn-green' %>
        </div>
      </div>
    </div>
  </div>
<% end %>

<div class="text-center">
  <%= link_to 'Back', bangsaku_path, class: 'btn btn-green' %>
</div>
